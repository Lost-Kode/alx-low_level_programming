#include "main.h"

/**
 * add - Adds two integers and returns the result.
 *
 * @x: number one.
 * @i: number two.
 *
 * Return: Add of number one and number two.
 */
int add(int x, int i)
{
		return (x + i);
}
