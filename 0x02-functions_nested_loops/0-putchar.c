#include <main.h>
/**
 * main -> print  _putchar in this function
 *
 * Return: always to 0
 */
int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		_putchar(i);
	}

	putchar('\n')
	return (0);
}
