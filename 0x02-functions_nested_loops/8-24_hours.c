#include "main.h"

/**
 * jack_bauer - Prints the minutes of a day
 *
 * Return: no return
 */
void jack_bauer(void)
{
	int k, o, d, e;

	for (k = 48; k <= 50; k++)
	{

		for (o = 48; o <= 57; o++)
		{
			for (d = 48; d <= 53; d++)
			{
				for (e = 48; e <= 57; e++)
				{
					if (a  >= 50 && b >= 52)
						break;
					_putchar(k);
					_putchar(o);
					_putchar(58);
					_putchar(d);
					_putchar(e);
					_putchar('\n');
				}
			}
		}
	}
}
