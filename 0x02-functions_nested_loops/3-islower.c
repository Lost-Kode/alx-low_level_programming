#include "main.h"
/**
 * _islower -> _islower checks for lowercase character
 * @c: a character argument
 * return: returns 1 and o which depends on the condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
